# 拾页星图：AI 资料整理规则

## 低 Token 选址流程

处理“向知识库添加内容”时，不要扫描整个 `library/`，也不要先输出全库文件清单。

1. 先阅读 `AI资料整理与放置指南.md`。
2. 根据资料标题和**知识模型、算法技巧、关键操作**运行：
   `python3 query_placement.py --title "<标题>" --keywords "<模型 技巧 操作>" --limit 3`
3. 查询只返回最多 3 个候选。优先检查第一候选的 `_说明.md`、直属子目录和同名冲突；不得因为工具方便扩大到全库扫描。
4. 置信度为 `medium` 或 `low`、可能存在重复项、或候选明显不合理时，读取 `KNOWLEDGE_ROUTING.md`；只有仍无法分类、需要跨分支查重或用户要求全库整理时，才读取 `KNOWLEDGE_STRUCTURE.md`。
5. 最终写入前必须局部确认目标父目录、目录元数据和冲突。无法可靠判断时，向用户提供不超过 3 个候选，不擅自放入“其他”。
6. 写入后等待服务自动刷新；服务未运行时执行 `python3 generate_structure.py`。验证 `/api/catalog`、两个快照的 revision、目标 `parent_path`、`primary_file` 和直属 `files`。

目录元数据可用 `placement: route|leaf` 明确是否接受未来子主题；省略时才使用自动判断。

## 学习页必须按内容独立设计

项目不提供可复制的学习页 HTML 成品模板。`docs/学习页安全与质量底线.md` 只记录离线 CSP、资源限制、可访问性和验收底线，**不得直接复制旧页面的 DOM、章节顺序或视觉结构后只替换文字**。

每道题先独立完成：

- 明确首次学习与复习分别需要恢复的思维链、认知难点和核心结论；
- 从题目模型选择内容结构与真正有教学作用的视觉锚点；
- 加载 `creative/frontend-design`（本机存在同名技能，必须使用完整限定名）；
- 若 `ui-skills-root` 可用，阅读 `docs/AI前端技能与可选工具.md`，用 `npx --yes ui-skills@0.2.4 list --category <类别>` 与 `get <作者/技能名>` 选择最具体的一个专项技能；复杂任务最多两个，与基础技能合计不超过三个；
- UI Skills、npm 或网络不可用时，直接使用本地前端设计技能和项目规范降级完成，不得阻塞任务、复制旧页面或临时接入 MCP；
- 对照近期 2～3 页只是为了避免重复，不是选择现成页面继续套用；
- 具体题目完整保留题面、输入、输出、范围和样例，题目模型另列；
- 用户声明 AC 的代码原样保留，HTML 完整代码从 CPP 确定性转义；
- 桌面、390px、深浅色、键盘、打印和严格 CSP 均需真实验证。

如果页面无法说明“为什么这种结构适合这道题、与近期页面至少两项差异在哪里”，不得交付。

## 边界

- `library/` 和 scanner catalog 是唯一事实来源；生成的 Markdown 不是第二份人工索引。
- 不读取日志、源码、历史截图或无关资料来完成一次小范围放置。
- 不覆盖、移动或删除用户源文件，除非用户明确批准并完成哈希核验。
- 公开仓库默认忽略个人知识资料；修改 `.gitignore` 白名单前先做隐私检查。
