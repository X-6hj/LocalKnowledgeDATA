# AI 资料整理与放置指南

> 适用项目：当前拾页星图（Folio Atlas）项目根目录；目录名可以是 GitHub 默认的 `LocalKnowledgeDATA`，也可以由使用者自行命名。
> 用途：用户已有文档时，未来 AI 先阅读本指南和自动生成的 `KNOWLEDGE_STRUCTURE.md`，即可完成目录选址、说明编写、复制和验证，无需重新扫描整个项目。

## 1. 工作边界

默认只读取：

1. 本指南；
2. 项目根目录自动生成的 `KNOWLEDGE_STRUCTURE.md`；
3. 用户明确提供的源文件或源文件夹；
4. `templates/_分类.md`、`templates/_说明.md`、`templates/学习笔记.html` 与 `docs/HTML学习笔记设计规范.md`；
5. 准备写入的目标目录及其必要的父目录说明；
6. 可能发生同名冲突的直属目录。

**不要为了放置几份文档而扫描整个项目、全部 `library`、源码、日志或历史截图。** 只有分类归属无法判断、疑似跨目录重复，或用户明确要求全库整理时，才扩大范围并说明理由。

默认操作是复制，不是移动或删除源文件。删除、覆盖、移动、改写原文档或合并同名文件前必须让用户决定。

正式资料唯一入口：

```text
<项目目录>\library\
```

### 全局结构快照

- `KNOWLEDGE_STRUCTURE.md` 是 `library/` 的完整目录、直属文件和目录摘要快照，路径固定，不是人工维护的第二份事实来源。
- 服务启动时必定同步生成一次；运行期间由服务端后台探测目录修订并按需更新，不依赖浏览器访问；内容相同时不改写。
- 不要编辑、复制出多个版本或把它提交到 Git。真实结构始终以 `library/` 为准。
- 服务未运行而资料刚发生变化时，先执行 `python3 generate_structure.py`；纯 Windows 使用 `runtime\windows-python\python.exe generate_structure.py`。
- 快照不存在或无法生成时，才退回到“目标父目录 + 候选直属目录”的最小范围检查；不要因此扫描源码、日志或整个项目。

## 2. 任意深度目录结构

```text
library/
└─ 一级主题/
   ├─ _分类.md                  # 可选：根索引名称、图标、顺序、摘要
   └─ 二级主题/
      ├─ _说明.md               # 可选：本级目录名称、摘要、标签、说明
      ├─ 三级主题/
      │  ├─ _说明.md
      │  └─ 原始文档.pdf
      └─ 本级附件.docx
```

规则：

- 目录树支持任意深度，不再限定“一级分类、二级条目”。
- 一级目录可以用 `_分类.md`；任意目录都可以用 `_说明.md`。
- 同一主题的主文档与附件放在最合适的同一目录。
- 只有存在真实主题关系时才继续嵌套，不创建无意义空层。
- 文件名尽量保留原名；名称明显无意义、含临时后缀或用户要求时才重命名。
- 算法具体题目目录统一使用“题号 - 题名 - 核心知识描述”，例如 `Problem I - 感染 - 传播转距离和与树重心`。描述应简短指出能学到的主要模型或技巧，不能只写题号和题名；题目目录内部不再建立“题目文件”等模糊子目录。
- 新题目或知识点默认同时保留“精致 HTML 首选学习页 + Markdown 备用版”；已有 AC 代码原样作为独立 CPP 保留，同时在 HTML 中完整展示。
- 具体题目笔记必须保留用户提供的完整题面，包括题目背景与任务、输入格式、输出格式、数据范围和样例；随后再单独提炼“题目模型”和解法主线。不得因为原文较长或复习笔记需要精简，就用题意摘要替代或删除完整题面。HTML 与 Markdown 两种版本都要保留这些内容；若用户提供的题面本身缺少某一项，应明确标注“源材料未提供”，不能猜测补写。
- Markdown 不内置数学渲染，公式使用反引号或 `text` 代码块写成可直接阅读的纯文本。HTML 可用语义化文字、公式块或原生 MathML，但不能依赖远程 MathJax/KaTeX。
- HTML 不是把 Markdown 包一层标签：生成前必须加载 `frontend-design`，按主题确定视觉方向和记忆锚点，并遵守 `docs/HTML学习笔记设计规范.md`。可参考已有高质量设计技能，但不得直接引入 CDN、脚本或不明第三方模板。
- `templates/学习笔记.html` 仅是安全语义骨架，禁止复制后只换标题和正文。生成前对照近期同类页面，新页面至少在首屏构图、内容化视觉锚点、章节节奏、配色版式四项中做两项明显差异；不默认使用英文眉题、机械编号或统一金句卡。
- HTML 的代码部分分为“关键函数/片段讲解”和“完整 AC 代码 + 整体执行流程”两层。完整代码从同目录 CPP 原样转义，不删改；整体讲解说明状态、调用顺序、输入输出和边界，不重复前面已经完成的函数级逐行解释。
- 每个题目目录的 `_说明.md` 用 `entry: 文件名.html` 指向首选页；不删除 Markdown 或其他文件。
- `_分类.md` 和 `_说明.md` 是元数据，不算普通附件。

## 3. 如何选择目录深度

优先使用“从宽到窄”的知识关系：

```text
算法 / 数论 / 质数 / 埃氏筛.cpp
计算机网络 / 传输层 / TCP / 拥塞控制.pdf
嵌入式 / STM32 / 外设 / ADC / 实验报告.docx
```

判断标准：

- 如果已有父目录能准确包含主题，放入该父目录下。
- 如果一组资料仍包含多个可独立扩展的子主题，再建立下一层。
- 如果目录只会放一个文件且没有继续扩展价值，可以直接放在当前父目录。
- 无法可靠判断时，列出不超过 3 个候选路径让用户决定，不擅自全库扫描。

## 4. 标准放置流程

1. **建立源文件清单**：只列用户指定范围内的正式文件，记录原路径、文件名和大小；忽略临时文件、下载残片、锁文件、快捷方式及测试文件。
2. **设计目标路径**：先从 `KNOWLEDGE_STRUCTURE.md` 获取全局框架，再根据主题关系选择所需层级；只补充查看目标父目录和可能重名的直属子目录。
3. **检查冲突**：无同名文件时准备复制；同名且哈希一致时跳过；同名但内容不同时停止并让用户选择保留、改名或覆盖。
4. **创建最少目录**：只创建当前资料需要的目录，不预建大量空分类或空层。
5. **先复制后核验**：保留源文件，比较源/目标字节数与 SHA-256，并从目标位置实际读取。
6. **生成学习页**：具体题目先完整保留题面、输入、输出、约束与样例，再将冗长题解压缩成便于复习的模型、推导和速记；HTML 与 Markdown 同时保留。HTML 展示完整 AC 代码并给出程序总览，关键函数仍单独讲解，但两部分不重复；不得擅自改写用户 AC 代码。纯知识点没有独立题面时不强行增加“题目描述”。
7. **编写说明**：任意新主题目录可添加 `_说明.md`，并用 `entry` 指向首选 HTML；一级目录需要根索引图标或顺序时添加 `_分类.md`。已有说明只做局部补充，不整份覆盖。
8. **刷新结构快照**：服务运行时等待自动同步；否则运行 `generate_structure.py`。确认固定快照包含新路径，且没有生成第二份编号文件。
9. **验证索引**：请求 `/api/catalog`，按 `folders[].path` 找到目标目录，确认 `primary_file` 正确且所有文件仍出现在直属 `files` 中。
10. **验证页面**：在“结构总览”中定位目标，再逐级进入目标目录，检查首选按钮、完整文件列表、面包屑和搜索；HTML 还要实测桌面、390px、深浅色与打印。
11. **维护记录**：批量迁移、重命名、覆盖或删除源文件时更新 `CHANGELOG.md`。

## 5. `_说明.md` 模板

```markdown
---
title: 本级目录的显示名称
tags: [主题标签, 文档类型]
pinned: false
order: 100
summary: 一句话说明本目录解决什么问题。
entry: 首选学习页.html
---

说明本目录的范围、主要内容、文件关系，以及建议的阅读或使用顺序。
```

要求：

- `title` 具体，不使用“资料”“文档1”等空泛名称；
- `summary` 只写已确认的信息；
- `tags` 保持 2～6 个，优先使用同一主题树中的已有词；
- 正文说明本级目录与子目录的分工；
- `entry` 只写本目录的直属文件名，不写路径；
- 不改动原文件正文。

一级目录的 `_分类.md` 可使用：

```markdown
---
title: 根目录显示名称
icon: ◇
order: 10
summary: 根目录的一句话说明。
---
```

## 6. 元数据优先级

- 任意目录说明：`_说明.md` → `_meta.md` → `说明.md` → `README.md`。
- 一级目录分类：`_分类.md` → `_category.md`。
- 一级目录同时存在分类与说明时，说明文件覆盖同名字段，未覆盖字段继续使用分类文件。

## 7. 完成前检查

- [ ] 只读取了必要指南、源文件、模板和目标路径；
- [ ] 先使用了 `KNOWLEDGE_STRUCTURE.md` 判断全局位置，没有无理由重扫整个知识库；
- [ ] 目录层级反映真实知识关系，没有固定套用两级结构；
- [ ] 没有覆盖内容不同的同名文件；
- [ ] 源文件仍在，除非用户明确批准移动或删除；
- [ ] 源/目标大小与 SHA-256 一致，目标文件可读取；
- [ ] 每级说明属于正确目录，编码为 UTF-8；
- [ ] `/api/catalog` 的 `folders[].path`、`parent_path` 和直属 `files` 正确；
- [ ] `primary_file` 指向首选 HTML，Markdown、CPP 与其他附件仍在完整文件列表；
- [ ] 具体题目的 HTML 与 Markdown 均保留完整题面、输入、输出、约束和样例，题目模型另列且没有替代原题；源材料缺失的部分已明确标注而非猜测补写；
- [ ] HTML 无远程依赖或脚本，且桌面、390px、深浅色、键盘与打印检查通过；
- [ ] HTML 中完整代码反向还原后与同目录 CPP 一致，并有不重复函数讲解的整体执行流程说明；
- [ ] HTML 与近期同类页面不存在“只换内容”的模板化关系，且没有明显 AI 套话或无意义装饰；
- [ ] 页面可逐级进入，面包屑正确，深层目录可搜索；
- [ ] 固定结构快照已刷新并包含目标路径，没有产生重复快照文件；
- [ ] 批量迁移或破坏性操作已更新日志。

## 8. 向用户汇报

```text
已放置：一级主题/二级主题/更深主题/文件名
源文件：原路径（已保留 / 已按批准移动）
说明文件：目标目录/_说明.md
校验：字节数一致；SHA-256 一致
索引：folders[].path 与直属 files 已确认
快照：KNOWLEDGE_STRUCTURE.md 已按需刷新
页面：结构总览、逐级导航、说明和搜索已确认
冲突或未完成项：无 / 具体说明
```

任何一步没有真实执行或验证，都必须写“未验证”并说明原因，不能根据目录结构猜测成功。
