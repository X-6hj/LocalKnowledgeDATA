# 更新日志

本项目遵循“功能变化写这里、运行过程写 `logs/server.log`”的原则。

## [1.3.1] - 2026-08-14

### 安全

- `/api/open` 现在拒绝非本机同源的 `Origin`，避免恶意网页跨站触发默认打开、选择应用或资源管理器定位；
- 文件操作接口只接受 `application/json`，使浏览器跨站请求必须预检，并阻断 `text/plain` 等简单请求绕过；
- 新增真实 HTTP 回归测试，确认跨站请求返回 403、简单内容类型返回 415，本机 `127.0.0.1` 与 `localhost` 同源请求保持兼容。

### 稳定性

- 客户端在 JSON 响应或文件传输期间提前断开时，服务安静结束该响应，不再输出 `BrokenPipeError` 或 `ConnectionResetError` 堆栈。

### 验证

- WSL Python 与 Windows 便携 Python 均 19/19 通过；
- Python 编译、JavaScript 语法和 ResourceWarning 严格检查通过。

## [1.3.0] - 2026-08-13

### 新增

- `library` 支持任意深度目录树，不再限制为一级分类和二级条目；
- `/api/catalog` 新增统一的 `folders` 节点模型，包含 `path`、`parent_path`、`depth`、直属文件和后代统计；
- 目录接口增加 `schema_version: 2`，前端会对旧服务给出明确的重启提示；
- 前端支持逐级进入目录、面包屑返回和当前目录说明区；
- 任意目录均可使用 `_说明.md`，目录卡片悬浮时显示本级说明；
- 全库搜索可直接命中任意深度目录；文件类型筛选可依据整个目录子树命中祖先目录。

### 兼容与维护

- 保留 `categories/items` 字段供旧客户端兼容；新功能以 `folders` 为准；
- 一级目录继续支持 `_分类.md`，并允许 `_说明.md` 覆盖同名显示字段；
- Windows PID 文件丢失时，停止器可通过 `run.py` 加项目路径或项目内 Python 运行时精确找回本项目服务；
- Windows 进程归属改为精确脚本边界或项目专用运行时匹配，不再接受同名前缀目录；
- 空目录新增、删除或重命名现在会改变 revision 并触发自动刷新；
- 扫描和 revision 共用可剪枝遍历，忽略隐藏目录、符号链接、断链和 Windows junction；
- 恢复旧 `categories/items/types` 的分类摘要、空条目过滤、深层更新时间和类型计数语义；
- 更新 README、使用手册、架构设计、维护与扩展、AI 资料整理指南和测试报告。

### 性能

- 扫描器改为自底向上线性聚合后代计数与文件类型，不再为每个目录重复遍历全部后代；
- 旧版条目兼容文件采用一次归属，不把深层文件列表复制到所有祖先；
- 前端建立 `path → folder` 与 `parent_path → direct children` 索引，普通导航只渲染当前直属子目录；
- 缓存标准化搜索文本，仅在全库搜索时遍历所有目录；
- 最终规模基准：1,040 目录/1,000 文件约 0.1592 秒，2,080 目录/2,000 文件约 0.2719 秒，增长比约 1.708。

### 验证

- 新增三级目录树、每级说明、空目录刷新、隐藏目录剪枝、链接隔离、旧接口契约、PID 丢失恢复和相对路径启动安全停止测试，WSL 和 Windows 便携 Python 均 15/15 通过；
- Chromium 实测“算法 → 数论 → 质数/最大公约数”逐级导航、面包屑、当前目录说明、深层悬浮说明和全库搜索；
- 根层 CPP 子树筛选只显示“算法”；
- 桌面 1440×900 与移动端 390×844 均无页面横向溢出；
- 验收截图：`qa/screenshots/nested-directory-navigation.png`、`qa/screenshots/nested-directory-mobile.png`。

## [1.2.1] - 2026-08-11

### 修复

- 长 `_说明.md` 在悬浮窗中出现滚动条后，鼠标离开卡片会立刻关闭窗口，导致无法滚动；
- 悬浮窗原先使用 `pointer-events: none`，不能接收滚轮、拖动滚动条或悬停事件。

### 改进

- 悬浮窗现在可以接收鼠标操作，并限制为纵向滚动；
- 鼠标从卡片移向悬浮窗时提供 320 ms 宽限时间，同时把两者间距缩小到 8 px；
- 鼠标进入悬浮窗会取消关闭计时，离开卡片和悬浮窗后才自动关闭；
- 增加滚动边界隔离和稳定滚动条槽，避免滚动长说明时带动页面或引发布局抖动。

### 验证

- 修复前复现：悬浮窗 `pointer-events: none`，移向悬浮窗后立即隐藏；
- 修复后 Chromium 行为测试：长说明可滚动，`scrollTop` 从 0 更新到 140，鼠标进入悬浮窗后保持显示，离开两者后自动隐藏；
- 验收截图：`qa/screenshots/hover-card-scroll-fixed.png`。

## [1.2.0] - 2026-08-11

### 新增

- 不依赖 WSL 的纯 Windows 启停入口：`start-windows.vbs/.bat` 与 `stop-windows.vbs/.bat`；
- 项目内置 Windows Python 3.14 便携运行时；
- `AI资料整理与放置指南.md`，供未来 AI 在不扫描整个项目的前提下整理用户已有文档；
- Windows 原生“选取应用”辅助进程与任务栏自动激活脚本。

### 修复

- “默认应用”不遵循当前 Windows 文件关联；
- “所在位置”错误跳到“文档”或只打开父目录，现在会精确选中目标文件；
- Windows“选取应用”只显示任务栏、未自动弹出；
- 激活选择器时出现蓝色 PowerShell 终端；
- Windows 嵌入式 Python 的 `_pth` 未包含项目根目录，导致纯 Windows 启动无法导入 `src`；
- Windows 单元测试因日志句柄未关闭而无法清理临时目录。

### 品牌与文档

- 页面正式更名为“拾页星图（Folio Atlas）”；原“知屿”是 AI 拟定的初始工作名，并非 GitHub 原项目名称；
- 当时为保持已有本地路径兼容，没有重命名原工作目录；程序本身不依赖目录名，公开仓库默认克隆目录为 `LocalKnowledgeDATA`；
- 同步更新 README、使用手册、维护与扩展、架构设计、故障排查和测试报告；
- 推荐启动方式切换为纯 Windows，WSL 启动器仅作兼容保留。

### 验证

- WSL Python：8/8 单元测试通过；
- Windows 便携 Python：8/8 单元测试通过；
- 纯 Windows `pythonw.exe` 进程、健康接口与停止流程真实通过；
- 默认打开由 Notepad 加载目标 TXT；
- 资源管理器 COM 证实目标文件被精确选中；
- 用户确认“选取应用”自动弹出、可以选择程序且无蓝色终端。

## [1.1.0] - 2026-08-11

### 新增

- 卡片“打开⌄”菜单：默认应用、选择应用、所在位置；
- 详情弹窗直接显示浏览器查看、默认打开、选择应用、所在位置四种操作；
- Windows“打开方式”选择器与资源管理器文件定位；
- “计算机网络”分类和《AI 辅助的 Linux 网络项目学习路线》真实资料。

### 改进

- 文件操作统一使用 `{ path, action }` API，并限制为 `default|choose|reveal`；
- Windows 命令全部使用参数列表或 `-LiteralPath`，不拼接不可信 shell 字符串；
- 桌面文件菜单浮层与移动端 2×2 详情操作按钮；
- 长标题、长文件名、390px 移动端排版适配。

### 迁移

- 从 `原网络学习目录` 迁移 19,579 字节、710 行的网络学习路线文档；
- 源/目标 SHA-256 均为 `79ca243bf169146050b17e1c927b38d6b2d0dcd67378b6e75aef9219044ab12b`；
- 校验成功后删除原正式文档，保留原目录中的 `write_test.md`。

### 验证

- 6 项单元测试通过；
- 真实 Windows “所在位置”和“选择应用”接口均返回 200；
- 桌面、详情弹窗和 390px 移动端无横向溢出。

## [1.0.0] - 2026-08-11

### 新增

- 基于文件夹的自动分类和知识条目扫描；
- `_分类.md` 与 `_说明.md` 元数据规范；
- 标题、说明、标签、文件名搜索；
- 分类、文件格式、收藏筛选和排序；
- 桌面悬浮说明与触屏详情弹窗；
- 浏览器查看和 Windows 默认应用打开；
- 5 秒级目录自动更新；
- 深色/浅色主题与响应式单双列布局；
- AI 生成的“知识群岛与档案索引”高清主视觉；
- 本地安全服务、Range 文件传输、CSP 与路径越界防护；
- 英文名 Windows 启停脚本；
- 说明模板、分类模板、示例资料；
- 使用、架构、维护、排错和测试文档；
- 运行日志自动轮转。

### 修复

- WSL 代理错误接管 localhost 健康检查导致 502；
- 浅色主题下主视觉标题对比度不足；
- Linux 无中文字体时的回退字体名称兼容；
- 浏览器收藏数据损坏时可能阻止页面启动；
- 缺少 favicon 导致的无意义 404。

### 验证

- Python/JavaScript 语法检查通过；
- 4 项核心单元测试通过；
- API、静态资源、Range 请求集成测试通过；
- 8 条目、2 分类的临时扩容排版测试通过；
- 1440px 桌面与 390px 移动端无横向溢出；
- 搜索、分类、收藏、详情、悬浮说明、主题切换通过；
- 临时资料移出后页面自动恢复正式目录。
