# 测试报告

- 最新回归日期：2026-08-20
- 环境：Windows 11 纯 Windows 便携 Python 3.14 + WSL Python 3.14.4 + Chromium
- 地址：`http://127.0.0.1:8765`

## 自动化结果

| 项目 | 结果 |
|---|---|
| Python 编译 | 通过 |
| JavaScript 语法 | 通过 |
| 单元测试 | 33/33 通过（本轮 WSL Python 3.14.4） |
| `/api/health` | 200，版本 `1.5.1` |
| `/api/catalog` | 200 |
| HTML/CSS/JS 静态资源 | 全部 200 |
| 示例文件 Range 请求 | 206，`bytes 0-9/275` |
| 路径越界拦截 | 通过 |
| 客户端提前断开 | 安静结束响应，不输出连接断开异常堆栈 |

## 1.5.1 全库星图交互完善

本轮使用独立临时端口 `18767` 和本地 Chromium 验收，没有占用默认端口 `8765`。真实 catalog 为 41 个目录、38 个文件、最大深度 5。

| 项目 | 实际结果 |
|---|---|
| 指针调宽 | 1440×900 下把左边缘从 x=870 拖到 x=700，抽屉由 570px 增至 740px；`aria-valuenow` 和 `kb:structure-width` 同步为 740 |
| 键盘调宽 | 分隔控件聚焦后按 `←`，宽度按 32px 步长变为 772px，焦点仍留在分隔控件 |
| 记忆与复位 | 刷新页面后宽度保持 772px；双击左边缘恢复 570px，本地存储同步更新 |
| 搜索内折叠 | 搜索“欧拉函数”命中 3 个目录并显示 4 个相关坐标；折叠“算法专题”后可见节点由 4 降至 1，再展开恢复为 4 |
| 深层目录定位 | 从“算法/数论/欧拉函数”重新打开总览，祖先链展开，当前项在结构滚动区内可见；主标题与面包屑保持“欧拉函数 / 知识库 › 算法专题 › 数论 › 欧拉函数” |
| 宽度持久化边界 | 桌面抽屉保持 570px 默认宽度且页面横向溢出为 0；可调范围通过 ARIA 暴露并随视口限幅 |
| 390px 移动端 | 对话框为 390×844；调宽手柄 `display:none`、`tabIndex=-1`、`aria-disabled=true`；页面和结构树横向溢出均为 0 |
| 视觉检查 | 深色/浅色主题下工具栏、层级线、当前位置高亮、背景遮罩和左边缘均无重叠或裁切；移动端按钮完整、目录文本按设计截断 |
| 自动化 | Python 编译、JavaScript 语法、差异空白检查通过；33/33 单元测试通过 |

## 1.5.0 全局结构快照与网页总览

本轮使用独立临时端口 `18766` 验收，没有停止或替换原先占用 `8765` 的用户服务。真实 catalog 为 41 个目录、38 个文件、最大深度 5。

| 项目 | 实际结果 |
|---|---|
| 固定快照 | 根目录只存在 1 个 `KNOWLEDGE_STRUCTURE.md`，共 94 行；已由 Git 忽略 |
| 重复生成 | 连续两次执行 `python3 generate_structure.py` 均返回“无需改写”，没有产生编号、日期或临时副本 |
| 启动生成 | `create_server()` 在浏览器可用前生成快照；服务健康接口返回 200 / `1.5.0` |
| 故障降级 | 模拟快照目录只读时 catalog 仍正常返回，日志记录 warning |
| 桌面首次打开 | 1440×900 下渲染 41/41 个目录，23 个有后代的分支全部展开，文件默认隐藏，搜索框自动获得焦点 |
| 文件开关 | 勾选后显示 38/38 个文件，总览内容继续在独立滚动区内浏览 |
| 结构内搜索 | 搜索“欧拉函数”保留 4 级相关路径，命中 3 个目录并显示 3 个同名相关文件 |
| 跳回逐级导航 | 点击“算法/数论/欧拉函数”后关闭总览，标题为“欧拉函数”，面包屑为“知识库 › 算法专题 › 数论 › 欧拉函数” |
| DOM 与焦点 | 关闭后结构树子节点数为 0，焦点回到“结构总览”入口；Esc 行为一致 |
| 桌面宽度 | `body.scrollWidth == innerWidth == 1440` |
| 移动宽度 | 对话框为 390×844，`body.scrollWidth == innerWidth == 390`，结构树 `scrollWidth == clientWidth == 359` |
| 移动滚动 | 目录树可视高度 548px、内容高度 1829px；滚动到最大值 1281 后最后一个“质数算法”完整可见 |

视觉检查确认：桌面端右侧星图抽屉不挤压主页面，背景柔化后层级明确；移动端头部、搜索、44px 工具按钮、独立目录滚动区和底部提示均无重叠或裁切。验收截图保存在本地 `qa/screenshots/global-structure-desktop.png`、`global-structure-mobile.png` 与 `global-structure-mobile-bottom.png`，均由 `.gitignore` 排除。

## 1.4.1 完整代码与程序总览

| 项目 | 实际结果 |
|---|---|
| Generate 01 String 完整代码 | HTML 反向还原后与同目录 CPP 的 1,470 个字符完全一致，共显示 108 行（包含结尾空行） |
| Problem I - 感染完整代码 | HTML 反向还原后与同目录 CPP 的 1,418 个字符完全一致，共显示 111 行（包含结尾空行） |
| 原有函数讲解 | 两页均继续保留独立的 `code` 章节；Generate 保留 `dfs()`，感染题保留 `getsiz()` 与 `center()` |
| 程序整体讲解 | 两页均新增 3 段程序总览，分别说明状态/初始化、`solve()` 调用主线和输出/边界，不重复函数区逐行内容 |
| 模板契约 | 自动化检查模板包含 `full-code`、`complete-code`、“完整 AC 代码”和“整体执行流程” |
| 桌面布局 | Generate 01 String 在 1440×900 下 `scrollWidth == innerWidth`，代码区与程序总览无重叠或截断 |
| 移动布局 | 感染题在 390×844 下 `scrollWidth == innerWidth`，总览收为单列，完整代码正常连续显示 |
| 打印 | Chromium 成功导出包含完整代码的 PDF，文件大小 992,640 字节 |
| 题目代码 | 按用户要求未重新编译、运行或随机对拍 AC 代码；只验证 HTML 展示与 CPP 内容一致 |

本轮使用独立临时端口 `18765` 验收版本 `1.4.1`，没有停止或替换原先占用 `8765` 的用户服务。验收截图保存在本地 `qa/screenshots/generate-full-code-desktop-final.png` 与 `qa/screenshots/infection-full-code-mobile-final.png`。

## 1.4.0 HTML 学习页与首选入口

| 项目 | 实际结果 |
|---|---|
| 自动首选 | 本目录存在 HTML/HTM 时，`primary_file` 自动指向按名称排序的第一个 HTML |
| 显式首选 | `_说明.md` 的 `entry` 可以覆盖自动选择；只接受本目录直属文件名 |
| 路径隔离 | 含 `/` 或 `\\` 的 `entry` 不会越级引用；无有效 HTML 时保持为空 |
| 文件兼容 | 首选入口之外，HTML、Markdown、CPP 等仍全部保留在 `files` 和直属文件列表 |
| 目录页入口 | 390×844 实测显示 1 个“打开学习笔记”入口和 3 个直属文件，未误显示空状态 |
| 页面宽度 | 主站与两份学习页在 390px、1440px 下均满足 `scrollWidth == innerWidth` |
| 公式显示 | 感染题跨边变化完整显示为 `S(v) − S(u) = [2(n − 1) − (2k − 1)] − (2k − 1) = 2(n − 2k)` |
| 离线与执行面 | 两份 HTML 均不含脚本、内联样式、事件处理器、HTTP/HTTPS 外链 |
| HTML CSP | `script-src 'none'`、`connect-src 'none'`、`object-src 'none'`，同时限制表单、基址和嵌入 |
| 键盘焦点 | 首个链接获得 3px 可见焦点轮廓 |
| 打印 | Chromium 成功导出学习页 PDF，文件大小 813,094 字节 |
| 视觉验收 | Generate 01 String 使用前缀和平衡边界图；感染题使用树重心图，不使用无关通用装饰 |

本轮还修复了一个既有显示问题：当前目录只有直属文件、没有子目录时，页面曾同时显示“没有找到对应资料”。现已由前端契约测试覆盖，目录存在直属文件时空状态保持隐藏。

本轮验收截图保存在本地 `qa/screenshots/generate-note-desktop-final.png`、`qa/screenshots/html-primary-entry-mobile-final.png` 和 `qa/screenshots/infection-note-mobile-final.png`。个人题目资料与验收截图遵守 `.gitignore`，不随公开仓库提交。

## 1.3.1 本机文件操作跨站防护

| 请求场景 | 实际结果 |
|---|---|
| `Origin: https://attacker.example` + `application/json` | 403，未执行文件操作 |
| 无 `Origin` + `text/plain` 简单请求 | 415，未执行文件操作 |
| 本机 `http://127.0.0.1:<端口>` + `application/json` | 200，保持兼容 |
| 健康接口版本 | `1.3.1` |
| JSON/文件响应期间客户端断开 | 捕获 `BrokenPipeError` 与 `ConnectionResetError`，服务继续运行 |

测试使用真实临时 HTTP 服务并替换最终系统打开动作，只记录是否进入执行路径；跨站和简单内容类型请求的动作记录均为空。服务仍只监听本机，路径边界检查继续作为第二道防线。

## 1.3.0 任意深度目录与性能

临时验收树：

```text
算法/
├─ 数论/
│  ├─ 质数/埃氏筛.cpp
│  └─ 最大公约数/欧几里得.cpp
└─ 组合数学/
```

| 项目 | 实际结果 |
|---|---|
| API 目录模型 | 返回 5 个临时目录节点，最大深度 3，父路径正确 |
| 根目录浏览 | 只显示一级“算法”，不把深层节点一次性铺开 |
| 进入算法 | 只显示直属“数论、组合数学” |
| 进入数论 | 只显示直属“最大公约数、质数算法” |
| 面包屑 | `知识库 › 算法 › 数论`，可逐级返回 |
| 当前目录说明 | 显示“研究整数性质及其计算方法。” |
| 深层悬浮说明 | “质数算法”显示本级说明和第 3 层统计 |
| 全库搜索 | 搜索“质数算法”只显示深层目标目录 |
| 子树类型筛选 | 根目录选择 CPP 后只显示“算法” |
| DOM 控制 | API 共 9 个目录时，“数论”页面仅渲染 2 张直属子目录卡片 |
| 桌面布局 | 1440×900，无横向溢出 |
| 移动布局 | 390×844，无页面横向溢出；根目录索引按设计横向滚动 |
| 空目录刷新 | 新增、重命名、删除空目录均改变 revision，服务缓存立即更新 |
| 隐藏目录 | 在隐藏祖先处剪枝，不生成全库搜索可见的孤儿节点 |
| 链接隔离 | 忽略目录符号链接、断链和 Windows junction，不读取库外元数据 |
| 旧接口契约 | 分类摘要、空条目过滤、深层更新时间和 `types` 计数口径保持旧语义 |
| PID 丢失恢复 | 按 `run.py` 加项目路径或项目内 Python 运行时核对后停止，拒绝模糊匹配其他 Python 进程 |

性能基准使用 `/tmp` 临时目录，不写入正式资料：

| 规模 | 扫描时间 |
|---|---:|
| 1,040 目录、1,000 文件 | 0.1592 秒 |
| 2,080 目录、2,000 文件 | 0.2719 秒 |

数据量翻倍时，扫描耗时增长比为 1.708。扫描器采用统一可剪枝遍历与自底向上聚合，前端使用路径与父路径索引，结果符合近似线性增长预期。

截图：`qa/screenshots/nested-directory-navigation.png`、`qa/screenshots/nested-directory-mobile.png`。

## 浏览器交互

| 功能 | 期望 | 实际 |
|---|---|---|
| 搜索“嵌入式” | 只显示对应卡片 | 1 张，正确 |
| 选择“示例资料” | 只显示示例 | 1 张，标题正确 |
| 收藏并仅看收藏 | 只显示收藏条目 | 1 张，状态正确 |
| 进入目录 | 显示本级说明、直属文件和直属子目录 | 逐级导航正确 |
| 桌面悬浮说明 | 显示 `_说明.md` | 标题与 144 字说明正确 |
| 主题切换 | dark/light 切换 | 通过 |
| 主视觉标题对比 | 使用浅色文字 | `rgb(237, 247, 250)` |

## 1.2.1 长说明悬浮窗滚动修复

| 项目 | 修复前 | 修复后 |
|---|---|---|
| 悬浮窗鼠标事件 | `pointer-events: none` | `pointer-events: auto` |
| 长说明滚动 | 有滚动条但无法把鼠标移入操作 | `scrollHeight > clientHeight`，实测 `scrollTop = 140` |
| 从卡片移向悬浮窗 | 立即隐藏 | 跨越间隙并进入悬浮窗后继续显示 |
| 同时离开卡片和悬浮窗 | — | 320 ms 后自动隐藏 |
| 视口边界 | — | 1440×900 截图中未超出视口 |

本轮使用真实 Chromium 加载页面并执行指针事件与滚动行为测试；修复前先得到失败结果，修复后再验证通过。截图保存在 `qa/screenshots/hover-card-scroll-fixed.png`。

## 1.2 纯 Windows、文件操作修复与重命名

| 项目 | 真实结果 |
|---|---|
| `start-windows.vbs` | 启动成功，健康接口返回 `ok: true`、`version: 1.2.1` |
| 实际服务进程 | `runtime\windows-python\pythonw.exe`，不依赖 WSL |
| `stop-windows.vbs` | 停止成功并清理 `.server.pid` |
| 默认应用打开 | `欢迎使用.txt` 由新 Notepad 进程打开，窗口标题正确 |
| 选择应用 | Windows 原生“选取应用”自动弹出；用户确认可选程序；无蓝色 PowerShell 窗口 |
| 所在位置 | 资源管理器 COM `SelectedItems()` 精确包含目标文件，不再只打开“文档” |
| 中文、空格路径 | 单元测试与真实文件均通过 |
| WSL 测试 | 8/8 通过 |
| Windows 便携 Python 测试 | 8/8 通过 |
| 正式品牌名 | “拾页星图（Folio Atlas）”；项目目录名保持不变 |

“选择应用”采用官方 `SHOpenWithDialog`。由于微软限制后台进程抢占前台，Windows 11 首先创建任务栏按钮；项目使用 UI Automation 找到 AutomationId 以 `OpenWith.exe` 结尾的真实任务栏按钮，单击后立即还原鼠标位置。激活 PowerShell 以 `CREATE_NO_WINDOW` 静默运行。

1.2.1 完成后再次执行纯 Windows 回归：`stop-windows.vbs` 停止检查通过，随后 `start-windows.vbs` 恢复服务；默认打开、所在位置、选择应用三个真实请求均返回 200；`../README.md` 路径越界和未知 action 均返回 400。

## 1.1 文件操作与真实资料

| 项目 | 结果 |
|---|---|
| 迁移资料索引 | 2 分类、2 条目、2 文件 |
| 迁移文档 HTTP 读取 | 200，19,579 字节 |
| 非法 action | 400，正确拒绝 |
| 卡片操作菜单 | 默认应用、选择应用、所在位置均显示 |
| 桌面详情按钮 | 4 项显示，无溢出 |
| 移动端详情按钮 | 2×2，每项 129px，无溢出 |
| Windows 资源管理器定位 | 真实调用，200 |
| Windows 选择应用 | 真实调用，200 |
| SHA-256 迁移核验 | 源/目标一致 |
| 原正式文档 | 核验后删除 |
| `write_test.md` | 保留 |

迁移文件 SHA-256：

```text
79ca243bf169146050b17e1c927b38d6b2d0dcd67378b6e75aef9219044ab12b
```

## 扩容与响应式

临时加入 7 个条目后总计 8 张卡片：

- 桌面 1440px：双列卡片、3 个分类按钮、无横向溢出；
- 移动 390×844：单列卡片，分类横向滚动，无横向溢出；
- 移出临时分类后：约 6 秒内自动恢复为 1 个条目和 2 个分类按钮。

## 已知说明

无头 Chromium 会把自己报告为“无悬浮设备”，因此浏览器测试时模拟了有鼠标环境来覆盖悬浮分支；真实 Windows 鼠标环境会直接触发。当前推荐运行方式是纯 Windows 便携 Python；WSL 只作兼容。所有原生文件操作先经过 `library` 路径边界检查，系统选择器激活会短暂移动鼠标到任务栏按钮并在约 80 ms 后恢复原位置。

## 截图证据

测试截图保存在 `qa/screenshots/`：

- 单条目桌面版；
- 8 条目桌面双列版；
- 8 条目 390px 移动单列版；
- 1.1 两条目桌面操作菜单；
- 1.1 桌面详情四操作按钮；
- 1.1 390px 移动操作菜单与详情按钮；
- 1.2.1 长说明悬浮窗滚动：`hover-card-scroll-fixed.png`。
- 1.3.0 三级目录导航：`nested-directory-navigation.png`；
- 1.3.0 390px 移动端：`nested-directory-mobile.png`。
